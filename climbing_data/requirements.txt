# Analysis + notebook dependencies for the climbing grade-density explorer.
# The one-time data export (see DATA.md) uses duckdb via OpenBeta's parquet-exporter;
# our own analysis code reads the resulting parquet with pandas + pyarrow.
pandas>=2.0
pyarrow>=14
duckdb>=1.0
matplotlib>=3.7
jupyter>=1.0
pytest>=7.0
